实验在本机 Python 里做（第 2 章的虚拟环境即可），**不需要 ROS**。可视化一律用仓库里的 notebook，不要另开一份脚本当主实验。每条先回答「要看懂什么」，再改 notebook 里对应单元。

- [ ] **实验 A：把 notebook 跑起来**
    - **要回答的问题**：本章的图从哪画？改一个角度后怎样重跑一格、而不是重跑全文件？
    - **步骤**：
        1. 在虚拟环境中安装：`pip install numpy matplotlib jupyter`
        2. 仓库根目录执行：
            ```bash
            jupyter notebook projects/ch04_linear_algebra/ch04_linear_algebra.ipynb
            ```
            或 VS Code / Cursor 直接打开该 `.ipynb`，选同一个内核。
        3. 先跑最上面的导入格。确认没有 `ModuleNotFoundError`。
        4. 习惯：改某格后只跑 **该格及依赖它的下一格**；图出现在格下方。菜单 Kernel → Restart 后必须从上到下再跑一遍。
    - **通过线**：导入格成功；你能指出「一格 = 一段可重复实验」。把启动方式写进笔记（Jupyter / Cursor 二选一即可）。

- [ ] **实验 B：向量、点积（notebook 第 1 节）**
    - **要回答的问题**：点积为 0 在图上看起来是什么？投影落在哪条线上？
    - **步骤**：按 notebook 说明画两个向量；改成题 1 的 \(a=(3,4),\ b=(4,-3)\)，打印点积与夹角。
    - **通过线**：图上两箭头近似垂直；打印的点积为 0。截图进笔记。

- [ ] **实验 C：矩阵把「小房子」送到哪，行列式是面积（第 2 节）**
    - **要回答的问题**：矩阵的两列和房子的底边、侧边如何对应？\(\det=-1\) 时房子为什么像镜子？
    - **步骤**：对题 4 的 \(A,B,C,D\) 各画一次房子；打印 `det`。
    - **通过线**：能指着 \(A\) 说「这是转了 \(90^\circ\)」；能指着 \(D\) 或 \(C\) 说「这是反射，不是旋转」。

- [ ] **实验 D：变换顺序（第 3 节，对应题 5）**
    - **要回答的问题**：同一个点，先转后移和先移后转，为什么到两个地方？
    - **步骤**：跑通甲、乙两套 \(T\)；在同一张图上画原点、原房子、甲之后、乙之后（颜色区分）。
    - **通过线**：甲把参考点送到约 \((1,1)\)，乙送到约 \((0,2)\)。口头讲清「平移发生在旋转前还是后」。

- [ ] **实验 E：齐次复合与求逆（第 4 节，本章硬指标）**
    - **要回答的问题**：\(T^{-1}T\) 离单位阵有多远？手写 \(R^\top,\ -R^\top t\) 是否和 `np.linalg.inv` 一致？
    - **步骤**：实现 `rot2`、`T2(theta, t)`、`invert_T`、`apply_T`。随机抽若干 \(\theta,t\)（或用题 6 的 \(T_1 T_2\)），计算 `np.max(np.abs(inv @ T - I))`。把一串变换画成：局部房子 → 乘 \(T_2\) → 再乘 \(T_1\)。
    - **通过线**：往返误差 \(< 10^{-12}\)（数量级，打印出来）。图上能看出复合与 \(T_{ac}=T_{ab}T_{bc}\) 一致（代码里才是 `T_ab @ T_bc`）。函数不要用 Python `for` 扫每个点，用一次矩阵乘（第 2 章约定）。

- [ ] **实验 F：纸笔题 1–8**
    - **要回答的问题**：不看图，数字能否独立算对？
    - **步骤**：先闭卷做笔记里的题，再在 notebook 最后一节用 NumPy 核对。参考折叠块最后再看。
    - **通过线**：8 题里至少 6 题数字对；题 5、题 6 必须对。错题抄进复盘。
