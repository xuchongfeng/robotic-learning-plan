# 第 4 章 线性代数可视化

对应笔记与题目：[第 4 章](../../docs/part1/ch04-linear-algebra.md)。

```bash
pip install -r requirements.txt
jupyter notebook ch04_linear_algebra.ipynb
```

在 Cursor / VS Code 里打开 `ch04_linear_algebra.ipynb` 选 Python 内核也可以。改一格就跑一格；Restart 之后要从上到下重跑。
